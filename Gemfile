source "https://rubygems.org"

gem "jekyll", "~> 3.8.5"

gem "webrick", "~> 1.8"

# Must match the plugins listed in _config.yml, otherwise a local build
# produces no sitemap. GitHub Pages installs its own allow-listed copy and
# does not read this file.
gem "jekyll-sitemap", "~> 1.4"
